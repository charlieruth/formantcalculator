Formant Calculator is a very simple practice project designed to be run in terminal. 

Folks wishing to use the app must compile the app using the g++ command: g++ PATH TO FILE -o FileName

The application is then run with ./FileName


For those unfamiliar with g++, you're encouraged to search g++ installation (it's easy, using xcode in macos terminal, homebrew, etc.)
